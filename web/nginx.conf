server {
    listen 80;
    server_name kindlepool.app www.kindlepool.app;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name kindlepool.app www.kindlepool.app;

    root /usr/share/nginx/html;
    index index.html;

    # SSL (terminated at reverse proxy or Cloudflare)
    ssl_certificate /etc/ssl/certs/kindlepool.pem;
    ssl_certificate_key /etc/ssl/private/kindlepool.key;

    # PWA manifest + service worker
    location ~* \.(json|js|css|png|jpg|svg|ico|webmanifest)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Service worker must not be cached
    location /sw.js {
        add_header Cache-Control "no-cache";
        expires -1;
    }

    # API proxy
    location /api/ {
        proxy_pass http://indexer:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # SPA fallback
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
}
